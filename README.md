# Pluck Homebrew Tap

```bash
brew tap RuochenLyu/pluck
brew install --cask pluck-app # the app
brew install pluck          # the CLI
```

Pluck removes image backgrounds entirely on-device. https://pluck.aix4u.com
